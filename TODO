- Better traces in event bus : maybe reduce other logging
- live_order.json: to check if it is becoming before order trigger
- IPC making a lots of load on system, to be shot using the threadpool only
- good work so far
- round up to 4 decimals
- should keep current quantity
- current change
- current step exit
- keeping this order in information, till we complete exit
- better naming in order object to understand in order
- current candle display in order
