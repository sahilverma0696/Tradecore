1. Check the updation of ltp and candle to the live order, making sure those values propogate ahead
2. Check for each update in live order, the exit manager checks if exit condition is true.
3. Check if exit manager propogates the exit signal ahead.
